apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fortress-external-integrations
  namespace: argocd
spec:
  project: default
  source:
    repoURL: .
    path: services/integrations
    targetRevision: HEAD
    directory:
      recurse: true
      include: "**/*.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: fortress-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
  revisionHistoryLimit: 3
