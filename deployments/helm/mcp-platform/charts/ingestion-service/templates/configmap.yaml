apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ingestion-service.fullname" . }}-config
  labels:
    {{- include "ingestion-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}