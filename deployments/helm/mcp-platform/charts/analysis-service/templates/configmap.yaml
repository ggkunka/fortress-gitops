apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "analysis-service.fullname" . }}-config
  labels:
    {{- include "analysis-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}