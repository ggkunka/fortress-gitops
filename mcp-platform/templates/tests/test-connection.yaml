apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcp-platform.fullname" . }}-test-connection"
  labels:
    {{- include "mcp-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcp-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
